# TODO

難易度目安: 小（数時間）/ 中（1〜2日）/ 大（数日以上）

## 未着手

- [ ] **「次の手を教える」の挙動改善** `難易度: 中`
  - 変更後の挙動:
    1. まずメモにミスがないか確認（確定済みセルと同じ行・列・ボックスにあるメモ数字を矛盾として指摘）
    2. 初級技法（Naked/Hidden Single）→中級（Locked Candidates/Pair/Triple）→上級（X-Wing等）の順に確定できるマスを提示
  - 表示形式（行・列・数字・技法名）は現行のまま
  - 影響: バックエンドのヒントAPIロジック (`src/solver/` 相当) + App.tsx の hintB 呼び出し

- [ ] **問題IDの表示と指定機能** `難易度: 中`
  - ゲーム中に問題IDを表示
  - 問題IDを入力すると同じ盤面を再現できる
  - 友人と同じ問題を共有・再挑戦が可能になる
  - ID形式・UIはシンプルで可（例: `#A3F9` 程度の短い英数字）
  - 影響: パズル生成にシード管理を追加、UI にID表示・入力欄を追加
  - 注意: 現行の生成ロジックがシードに対応しているか要確認

- [ ] **選択中セルの数字にも丸バッジを表示** `難易度: 小`
  - 現状: 他のセルにある同じ数字には青丸が付くが、フォーカス中のセル自体には付いていない
  - 変更: 選択中セルの数字にも同様の丸バッジを表示する
  - 影響: App.css のセレクタ（`:not(.cell--selected)` の除外を解除または調整）のみ

- [ ] **ダークモードの追加** `難易度: 小`
  - `prefers-color-scheme: dark` への自動対応、または手動トグル
  - 影響: App.css の CSS変数を上書きするだけで対応可能（変数設計済み）

- [ ] **Windowsからワンクリックで起動できるようにする** `難易度: 小〜中`
  - 目的: WSLを毎回起動してコマンドを叩かずに、デスクトップのショートカットなどから即起動したい
  - 候補:
    - **PWA化**: ブラウザの「アプリとしてインストール」でデスクトップに置ける（Viteプラグインで対応可）
    - **静的ビルド + .bat**: `npm run build` の成果物を `start index.html` するバッチファイルで開く（WSL不要）
    - **Electron化**: 専用ウィンドウで動くネイティブアプリ（規模が大きい）
  - 影響: ビルド設定の変更のみ（ゲームロジック・UIは無変更）

- [ ] **Killer Sudoku モードの実装** `難易度: 大`
  - 点線で囲まれたケージの合計値が制約として与えられるモード
  - 影響: パズル生成・ソルバー・UIすべてに大規模な変更が必要
